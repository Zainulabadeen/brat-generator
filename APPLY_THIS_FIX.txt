FINAL FIX FOR GROWING BLANK SPACE

1. Stop npm run dev with Ctrl+C.
2. Copy the components and lib folders from this update into your current project:
   E:\bratgenerator-build-fixed
3. Choose Replace files in destination.
4. Run: npm run dev
5. Test Home and /video-generator/ locally. Refresh each page several times.
6. If stable, stop dev and run: npm run prelive
7. If PASS:
   git add .
   git commit -m "Fix generator height growth after refresh"
   git push

What changed:
- Removed the document.scrollHeight feedback loop.
- Parent now measures only the actual widget root.
- Removed legacy height postMessage code from embedded tools.
- Removed forced minimum iframe heights that created artificial blank space.
