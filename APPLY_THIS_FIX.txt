Brat Generator first-page iframe fit fix

Replace:
  lib/embedDocuments.ts

This removes the embedded generator's own 20px top/bottom wrapper padding and duplicate inner border/shadow, so the tool sits flush inside the existing outer tool shell like the other tools.

Then run:
  npm run dev
