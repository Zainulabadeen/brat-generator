BRAT GENERATOR — TEXT WIDTH ONLY FIX (2026-09-23)

What this fixes:
- Restores wide/full section and card-grid layout across the site.
- Keeps only section heading + supporting paragraph at a readable centered width.
- Does NOT shrink cards, images, tools, section backgrounds, or main layout wrappers.
- Keeps the previous language-selector update unchanged (US, ID, PH, IN, MY, TR + globe icon).

Apply:
1. Extract this ZIP into the existing project root.
2. Replace files when Windows asks.
3. Run: npm run dev
4. Hard refresh localhost with Ctrl+F5.

Changed file:
- app/globals.css
