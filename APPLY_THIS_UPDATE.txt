HEADING STYLE RESET - CENTER ONLY

1. Copy the app folder from this update.
2. Paste it into your current E:\bratgenerator-build-fixed project.
3. Choose Replace files in destination.
4. Run: npm run dev

This update only resets decorative editorial heading styling:
- removes side mini-lines around eyebrow labels
- removes heading background wash
- removes green diamond/dot under headings
- removes extra decorative heading shadows/borders
- keeps headings, labels and editorial copy centered

The existing one-column Before You Start checklist is left unchanged.
